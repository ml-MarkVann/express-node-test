extends layout

block content
  h1= title
  
  ul
  each author, index in author_list
    li 
      a(href=author.url) #{author.name} 
      //- | (#{birth_list[index]} - #{death_list[index]})
      | #{author.lifespan}

  else
    li There are no authors.